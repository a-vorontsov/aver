func test(b : int) : int {
    let a : int = b + b;
    return a;
}

func main() : void {
    let x : int = 32;

    if (x == 2) {
        print x;
    } else {
        print 2;
    }

    let a : int = 4;
    print a;
    print test(a);
}
