func fib(n int) int {
    if (n <= 1) {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

func main() void {
    let i = 0;
    while (i <= 30) {
        println fib(i);
        i = i + 1;
    }
}
