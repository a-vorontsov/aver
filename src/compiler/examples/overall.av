struct Asdf {
    a : int;
    b : string;
}

func test(x int) void {
    if (x > 0) {
        println 1;
    } else {
        println 2;
    }
}

func main() void {
    let a = Asdf{
        a = 1;
        b = "hello";
    };
    println a.a;
    a.b = "world";

    let h int[][] = [[1,2], [3], []];

    println "Please enter a number:";
    test(input);
}