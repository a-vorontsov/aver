start = 1000;
x = start;
y = start;
z = start;
while (x > 0) {
    while (y > 0) {
        while (z > 0) {
            z = z - 1;
        }
        z = start;
        y = y - 1;
    }
    y = start;
    x = x - 1;
}
print 23;